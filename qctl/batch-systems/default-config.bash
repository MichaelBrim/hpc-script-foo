default_not_supported () {
    echo "echo ERROR - $1() not supported"
}

qctl_cancel_job () {
    default_not_supported ${FUNCNAME[0]}
}

qctl_cancel_queue () {
    default_not_supported ${FUNCNAME[0]}
}

qctl_detail_job () {
    default_not_supported ${FUNCNAME[0]}
}

qctl_detail_queue () {
    default_not_supported ${FUNCNAME[0]}
}

qctl_hold_job () {
    default_not_supported ${FUNCNAME[0]}
}

qctl_hold_queue () {
    default_not_supported ${FUNCNAME[0]}
}

qctl_resume_job () {
    default_not_supported ${FUNCNAME[0]}
}

qctl_resume_queue () {
    default_not_supported ${FUNCNAME[0]}
}

qctl_status_hosts () {
    default_not_supported ${FUNCNAME[0]}
}

qctl_status_job () {
    default_not_supported ${FUNCNAME[0]}
}

qctl_status_project () {
    default_not_supported ${FUNCNAME[0]}
}

qctl_status_queue () {
    default_not_supported ${FUNCNAME[0]}
}

qctl_status_user () {
    default_not_supported ${FUNCNAME[0]}
}

qctl_submit_batch () {
    default_not_supported ${FUNCNAME[0]}
}

qctl_submit_interactive () {
    default_not_supported ${FUNCNAME[0]}
}

